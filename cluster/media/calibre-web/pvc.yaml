apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-web-v2
  namespace: media
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
