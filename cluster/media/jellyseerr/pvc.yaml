apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyseerr-config-v1
  namespace: media
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
