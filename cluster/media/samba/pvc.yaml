kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: samba-v1
  namespace: media
spec:
  storageClassName: rook-cephfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
