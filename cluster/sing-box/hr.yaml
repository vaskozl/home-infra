# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sing-box
  annotations:
    patch.sko.ai/app-template: common
spec:
  interval: 24h
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 5.0.1
  values:
    controllers:
      sing-box:
        replicas: 2
        containers:
          app:
            image:
              repository: ghcr.io/sagernet/sing-box
              tag: v1.13.13@sha256:41c231813fc1584fcc4e8556ad839365aa47a1b730e370d89980d1878b9cdce0
            args: [run, -c, /etc/sing-box/config.json]
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              limits:
                memory: 150Mi
              requests:
                cpu: 30m
                memory: 150Mi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
              readOnlyRootFilesystem: true
        statefulset:
          podManagementPolicy: Parallel
        type: statefulset
    defaultPodOptions:
      securityContext:
        fsGroup: 568
        runAsGroup: 568
        runAsNonRoot: true
        runAsUser: 568
        seccompProfile:
          type: RuntimeDefault
    persistence:
      config:
        globalMounts:
          - path: /etc/sing-box
        name: config
        type: secret
    route:
      app:
        hostnames:
          - sko.ai
        parentRefs:
          - name: public
            namespace: agentgateway
        rules:
          - matches:
              - path:
                  value: /api/v2/stream
            backendRefs:
              - identifier: vless
                port: 8080

    service:
      vless:
        controller: sing-box
        ports:
          http:
            port: 8080
