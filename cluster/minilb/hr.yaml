---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: minilb
spec:
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 3.1.0
  values:
    controllers:
      controller:
        containers:
          app:
            image: &image
              repository: ghcr.io/vaskozl/minilb
              tag: 0.0.1@sha256:df07c9fc33e3aed7606eff470649cf394f6ed038d1ec442704e12e5d484eb3c4
            args:
            - -log-level=debug
            - -controller=true
            resources: &resources
              limits:
                memory: 10Mi
              requests:
                cpu: 5m
                memory: 10Mi
      dns:
        strategy: RollingUpdate
        replicas: 2
        containers:
          app:
            image: *image
            args:
            - -log-level=debug
            resources: *resources
    serviceAccount:
      name: "minilb"
    service:
      app:
        controller: dns
        type: NodePort
        ports:
          http:
            port: 53
            nodePort: 30053
            protocol: UDP
