# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fluent-aggregator
  annotations:
    patch.sko.ai/app-template: common
spec:
  interval: 24h
  chart:
    spec:
      chart: app-template
      interval: 24h
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 5.0.1
  values:
    controllers:
      fluent-aggregator:
        replicas: 1
        containers:
          app:
            image:
              repository: ghcr.io/vaskozl/fluent-bit
              tag: 5.0.6@sha256:22e8b4891d3be7a210af12980a49b6ae0da6d4352767b982b1713831cbc4b0b8
            args: [-c, "/config/fluent-bit.yaml"]
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              limits:
                memory: 128Mi
              requests:
                cpu: 15m
                memory: 64Mi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
              readOnlyRootFilesystem: true
        statefulset:
          podManagementPolicy: Parallel
        type: statefulset
    defaultPodOptions:
      securityContext:
        fsGroup: 2000
        runAsGroup: 2000
        runAsUser: 2000
        seccompProfile:
          type: RuntimeDefault
    persistence:
      config:
        name: aggregator-config
        type: configMap
      logs:
        existingClaim: logging-pvc
        globalMounts:
          - path: /var/log/k8s
      flb-storage:
        type: emptyDir
        globalMounts:
          - path: /flb-storage
    service:
      app:
        controller: fluent-aggregator
        ports:
          forward:
            port: 24224
          http:
            port: 2020
