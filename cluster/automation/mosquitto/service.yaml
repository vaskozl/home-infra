---
apiVersion: v1
kind: Service
metadata:
  name: mosquitto
spec:
  ports:
  - name: mqtt
    port: 1883
    targetPort: 1883
  selector:
    app.kubernetes.io/name: mosquitto
  type: LoadBalancer
  externalTrafficPolicy: Local
  externalIPs:
  - ${EI_MQTT}
