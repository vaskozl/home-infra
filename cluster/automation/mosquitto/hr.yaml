apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: mosquitto
  annotations:
    patch.sko.ai/app-template: statefulset
    patch.sko.ai/app-security: restricted
spec:
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    controller:
      replicas: 2
    image:
      repository: ghcr.io/vaskozl/mosquitto
      tag: 2.0.18@sha256:2ec265e1c6261593f1582a6ab54a30f8773de704da3c7c1bc6b05aa9d1a894c0
    args: [sh, -c, 'exec mosquitto -c "/config/$(hostname).conf"']
    service:
      main:
        annotations:
          coredns.io/hostname: mqtt
        ports:
          http:
            enabled: false
          mqtt:
            enabled: true
            port: 1883
        type: LoadBalancer
        externalTrafficPolicy: Local
        externalIPs:
        - ${EI_MQTT}
    persistence:
      config:
        enabled: true
        type: configMap
        name: mosquitto-config
    resources:
      limits:
        memory: 200Mi
      requests:
        cpu: 2m
        memory: 8Mi
