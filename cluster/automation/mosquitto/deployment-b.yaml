---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mosquitto-b
spec:
  selector:
    matchLabels:
      app: mosquitto
  template:
    metadata:
      labels:
        app: mosquitto
        type: bridge
    spec:
      containers:
        - image: public.ecr.aws/docker/library/eclipse-mosquitto:2.0.15@sha256:00fefa7b08cd368d5ee1a894ccb98c269f3837d83207a026483a37cc67a0db1c
          name: mosquitto
          ports:
            - containerPort: 1883
            - containerPort: 9001
          volumeMounts:
          - mountPath: /mosquitto/config
            name: config
          command:
            - mosquitto 
          args:
            - -c 
            - /mosquitto/config/mosquitto.conf
      volumes:
      - name: config
        configMap:
          name: bridge-conf
      securityContext:
        runAsUser: 1883
        runAsGroup: 1883
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values: [mosquitto]
            topologyKey: kubernetes.io/hostname
