---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: k10
  namespace: kasten-io
spec:
  releaseName: k10
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://charts.kasten.io/
      chart: k10
      version: 4.0.5
      sourceRef:
        kind: HelmRepository
        name: kasten-charts
        namespace: flux-system
      interval: 5m
  values:
    eula:
      accept: true
      company: Vasko
      email: "devnull@sko.ai"
    global:
      persistence:
        storageClass: rook-ceph-block
    auth:
      tokenAuth:
        enabled: false
    clusterName: k8s
    #    ingress:
    #      create: true
    #      host: "k10.sko.ai"
    #      urlPath: k10
    #      hosts:
    #      - "k10.sko.ai"
    #      tls:
    #        enabled: true
    #        secretName: "cert-manager/sko-ai-tls"
