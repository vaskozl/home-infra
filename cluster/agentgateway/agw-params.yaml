apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayParameters
metadata:
  name: agw-params
spec:
  deployment:
    spec:
      replicas: 2
  service:
    spec:
      type: LoadBalancer
      loadBalancerClass: minilb
