kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: qbittorrent
  annotations:
    volume.beta.kubernetes.io/storage-class: "rook-ceph-block"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
