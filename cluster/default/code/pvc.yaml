kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: code-server
  annotations:
    volume.beta.kubernetes.io/storage-class: "longhorn"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
