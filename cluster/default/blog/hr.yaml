apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: blog
  annotations:
    patch.sko.ai/app-template: enabled
spec:
  values:
    controller:
      replicas: 2
      strategy: RollingUpdate
    image:
      repository: registry.sko.ai/vasko/blog
      tag: head@sha256:b4b8a6cbea2103833ed1a0439b58d9e17fe172bedb5644c31476adaa91edcd93
    imagePullSecrets:
    - name: regcred
    service:
      main:
        ports:
          http:
            port: 8080
    ingress:
      main:
        enabled: true
        ingressClassName: external
        annotations:
          nginx.ingress.kubernetes.io/enable-global-auth: 'false'
          nginx.ingress.kubernetes.io/configuration-snippet: |
            add_header Cache-Control "public";
        hosts:
        - host: sko.ai
          paths:
          - path: /
    resources:
      limits:
        cpu: 100m
        memory: 50Mi
      requests:
        cpu: 2m
        memory: 10Mi
