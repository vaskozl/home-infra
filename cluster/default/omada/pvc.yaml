---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: omada-pvc-v3
  namespace: default
spec:
  storageClassName: ssd-raid5
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
