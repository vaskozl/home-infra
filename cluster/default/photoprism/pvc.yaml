kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: photoprism
  annotations:
    volume.beta.kubernetes.io/storage-class: "rook-ceph-block"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 12Gi
