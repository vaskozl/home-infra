apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: microbin
  annotations:
    patch.sko.ai/app-template: statefulset
    patch.sko.ai/app-security: restricted
spec:
  values:
    image:
      repository: ghcr.io/vaskozl/microbin-bin
      tag: 2.0.4@sha256:2bfedd59434419b5d221aecd5f57348dd62060aabdd4d49d38eab0492ac90148
    env:
      MICROBIN_PORT: &port 8080
      MICROBIN_DATA_DIR: &data /var/lib/microbin
    envFrom:
    - secretRef:
        name: microbin.env
    volumeClaimTemplates:
    - name: data
      mountPath: *data
      accessMode: ReadWriteOnce
      size: 1Mi
    service:
      main:
        ports:
          http:
            port: *port
    ingress:
      main:
        enabled: true
        ingressClassName: haproxy-external
        hosts:
        - host: mb.sko.ai
          paths:
          - path: /
    resources:
      limits:
        memory: 80Mi
      requests:
        cpu: 10m
        memory: 10Mi
