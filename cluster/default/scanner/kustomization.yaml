apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deploy.yaml
  - rbac.yaml
  - alerts.yaml
labels:
  - pairs:
      app: scanner
    includeSelectors: true
configMapGenerator:
  - name: script
    files:
      - scan-containers.pl
