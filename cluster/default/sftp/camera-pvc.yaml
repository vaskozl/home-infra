kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: camera
  annotations:
    volume.beta.kubernetes.io/storage-class: "longhorn"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
