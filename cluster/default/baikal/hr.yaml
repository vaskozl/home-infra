apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: baikal
  annotations:
    patch.sko.ai/app-template: statefulset
spec:
  values:
    image:
      repository: ghcr.io/vaskozl/baikal
      tag: 0.9.4@sha256:96e06e1ecb1949839b1103fc427541af09fbd860e33f80c96da14fd5ed96b6df
    volumeClaimTemplates:
    - name: data
      mountPath: /var/lib/baikal
      accessMode: ReadWriteOnce
      size: 1Mi
    persistence:
      msmtprc:
        enabled: true
        type: configMap
        name: msmtprc
        mountPath: /etc/msmtprc
        subPath: msmtprc
    service:
      main:
        ports:
          http:
            port: 80
    ingress:
      main:
        annotations:
          nginx.ingress.kubernetes.io/enable-global-auth: 'false'
        enabled: true
        hosts:
        - host: bk.sko.ai
          paths:
          - path: /
    resources:
      limits:
        memory: 1Gi
      requests:
        cpu: 10m
        memory: 130Mi
