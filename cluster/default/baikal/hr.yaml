# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  annotations:
    patch.sko.ai/app-template: common
  name: baikal
spec:
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 4.6.1
  interval: 24h
  values:
    controllers:
      baikal:
        containers:
          app:
            env:
              SMTPHOST: mx-maddy.mailserver.svc.cluster.local:2225
            image:
              repository: ghcr.io/vaskozl/baikal
              tag: 0.10.1@sha256:4c875b012a96128b9e3d71ce0abac5eb2bb45dffe0ecdcc41f3cfef602bc4f0b
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              limits:
                memory: 60Mi
              requests:
                cpu: 25m
                memory: 60Mi
        statefulset:
          podManagementPolicy: Parallel
          volumeClaimTemplates:
            - accessMode: ReadWriteOnce
              globalMounts:
                - path: /usr/share/webapps/baikal/Specific/db
                  subPath: db
                - path: /usr/share/webapps/baikal/config
                  subPath: config
              name: data
              size: 1Mi
        type: statefulset
    persistence:
      nginx:
        globalMounts:
          - path: /etc/nginx/nginx.conf
            subPath: nginx.conf
        name: nginx
        type: configMap
    route:
      app:
        annotations:
          gethomepage.dev/description: Calendar and contacts with invites
          gethomepage.dev/enabled: 'true'
          gethomepage.dev/group: Cloud
          gethomepage.dev/icon: baikal
          gethomepage.dev/name: Baïkal
        hostnames:
          - bk.sko.ai
        parentRefs:
          - name: internal-noauth
            namespace: agentgateway
    service:
      app:
        controller: baikal
        ports:
          http:
            port: 80
