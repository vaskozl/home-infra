---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: baikal
spec:
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 3.1.0
  values:
    controllers:
      baikal:
        containers:
          app:
            image:
              repository: ghcr.io/vaskozl/baikal
              tag: 0.9.5@sha256:bb88aabb2e116cbc6d7076e1af01e4b1512905fca258d66f44b9c8640a8b6455
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              limits:
                memory: 60Mi
              requests:
                cpu: 25m
                memory: 60Mi
        statefulset:
          podManagementPolicy: Parallel
          volumeClaimTemplates:
          - accessMode: ReadWriteOnce
            globalMounts:
            - path: /var/lib/baikal
            name: data
            size: 1Mi
        type: statefulset
    ingress:
      app:
        hosts:
        - host: bk.sko.ai
          paths:
          - path: /
            service:
              identifier: app
              port: http
    persistence:
      msmtprc:
        globalMounts:
        - path: /etc/msmtprc
          subPath: msmtprc
        name: msmtprc
        type: configMap
    service:
      app:
        controller: baikal
        ports:
          http:
            port: 80
