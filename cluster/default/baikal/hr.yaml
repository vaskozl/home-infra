---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: baikal
spec:
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 3.1.0
  values:
    controllers:
      baikal:
        containers:
          app:
            image:
              repository: ghcr.io/vaskozl/baikal
              tag: 0.9.5@sha256:de81e30f4136ad01d79a20b8b2a2bbd97447306b75301d072e8b145c7e4aadd2
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              limits:
                memory: 1Gi
              requests:
                cpu: 10m
                memory: 130Mi
        statefulset:
          podManagementPolicy: Parallel
          volumeClaimTemplates:
          - accessMode: ReadWriteOnce
            globalMounts:
            - path: /var/lib/baikal
            name: data
            size: 1Mi
        type: statefulset
    ingress:
      app:
        hosts:
        - host: bk.sko.ai
          paths:
          - path: /
            service:
              identifier: app
              port: http
    persistence:
      msmtprc:
        globalMounts:
        - path: /etc/msmtprc
          subPath: msmtprc
        name: msmtprc
        type: configMap
    service:
      app:
        controller: baikal
        ports:
          http:
            port: 80
