---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: baikal-v1
  namespace: default
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 300Mi
