kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: radicale
  annotations:
    volume.beta.kubernetes.io/storage-class: "rook-ceph-block"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 300Mi
