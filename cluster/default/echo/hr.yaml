# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: echoserver
  annotations:
    patch.sko.ai/app-template: common
spec:
  interval: 24h
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 4.6.2
  values:
    controllers:
      echoserver:
        containers:
          app:
            image:
              repository: ghcr.io/vaskozl/openresty
              tag: 1.27.1@sha256:e8de730b786346e67fe5f4422d5c5ed7f816dd8f4291e9e70f27aa3563bd2cc3
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              limits:
                memory: 50Mi
              requests:
                cpu: 10m
                memory: 20Mi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
              readOnlyRootFilesystem: true
    defaultPodOptions:
      securityContext:
        fsGroup: 568
        runAsGroup: 568
        runAsNonRoot: true
        runAsUser: 568
        seccompProfile:
          type: RuntimeDefault
    route:
      app:
        parentRefs:
          - name: internal
            namespace: agentgateway
        hostnames:
          - echo.sko.ai
    persistence:
      config:
        globalMounts:
          - path: /etc/nginx/nginx.conf
            subPath: nginx.conf
        name: echo-conf
        type: configMap
      tmp:
        type: emptyDir
    service:
      app:
        controller: echoserver
        ports:
          http:
            port: 8080
