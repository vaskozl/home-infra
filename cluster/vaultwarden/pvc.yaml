apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vaultwarden-data-v4
  namespace: vaultwarden
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
