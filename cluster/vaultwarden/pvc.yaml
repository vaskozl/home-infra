---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: vaultwarden-data-v2
  namespace: vaultwarden
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
