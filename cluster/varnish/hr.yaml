---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  annotations:
    patch.sko.ai/app-template: statefulset
  name: varnish
spec:
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 3.1.0
  values:
    args:
    - -f
    - /config/main.vcl
    - -F
    - -a
    - :6081
    - -s
    - malloc,384M
    - -t
    - '7200'
    controller:
      replicas: 2
    controllers:
      varnish:
        containers:
          app:
            resources:
              limits:
                cpu: 1
                memory: 512Mi
              requests:
                cpu: 15m
                memory: 512Mi
    image:
      repository: ghcr.io/vaskozl/varnish
      tag: 7.5.0@sha256:8c988c36c0274efe2b2c974dd3ff0960cee1b812f3b6c50f6c72f9dac62fecd7
    ingress:
      main:
        annotations:
          haproxy.org/load-balance: uri
        enabled: true
        hosts:
        - host: varnish.sko.ai
          paths:
          - path: /
        ingressClassName: haproxy-internal
    persistence:
      config:
        enabled: true
        name: varnish-config
        type: secret
      varnishd:
        enabled: true
        mountPath: /var/lib/varnish/varnishd
        type: emptyDir
    podSecurityContext:
      fsGroup: 4000
      runAsGroup: 4000
      runAsUser: 4000
    service:
      main:
        enabled: true
        ports:
          http:
            enabled: true
            port: 6081
