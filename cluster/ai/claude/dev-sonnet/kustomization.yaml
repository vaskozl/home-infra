# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - sts.yaml
components:
  - ../components/common
  - ../components/chromium
  - ../components/worker
labels:
  - pairs:
      app.kubernetes.io/instance: claude-worker-sonnet
    includeSelectors: true
