# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/master/statefulset-apps-v1.json
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: common
spec:
  serviceName: claude
  volumeClaimTemplates:
    - metadata:
        name: claude
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 10Gi
  template:
    spec:
      serviceAccountName: claude
      hostUsers: false
      shareProcessNamespace: true
      securityContext:
        fsGroup: 568
        fsGroupChangePolicy: OnRootMismatch
      nodeSelector:
        kubernetes.io/arch: amd64
      containers:
        - name: claude
          image: ghcr.io/vaskozl/claude:latest@sha256:552e07194e4553afae67b126644d3c53f92e1cf6cc8b9c73ad90ab3fc5a568db
          securityContext:
            procMount: Unmasked
            seccompProfile:
              type: Unconfined
            allowPrivilegeEscalation: true
            runAsUser: 568
            runAsGroup: 568
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              memory: 2.5Gi
          envFrom:
            - secretRef:
                name: claude-env
          volumeMounts:
            - name: claude
              mountPath: /home/nonroot
            - name: ralph
              mountPath: /usr/local/bin/ralph-dev
              subPath: ralph-dev.sh
            - name: ralph
              mountPath: /usr/local/bin/ralph-lead
              subPath: ralph-lead.sh
            - name: ralph
              mountPath: /usr/local/bin/ralph-dx
              subPath: ralph-dx.sh
            - name: ralph
              mountPath: /etc/claude/prompt.md
              subPath: prompt.md
            - name: ralph
              mountPath: /usr/local/lib/ralph-common.sh
              subPath: ralph-common.sh
            - name: buildkit-certs
              mountPath: /etc/buildkit/certs
              readOnly: true
      volumes:
        - name: buildkit-certs
          secret:
            secretName: buildkit-client-certs
