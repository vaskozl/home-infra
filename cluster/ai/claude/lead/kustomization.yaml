# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - sts.yaml
  - scaledobject.yaml
labels:
  - pairs:
      app.kubernetes.io/instance: claude-lead-opus
    includeSelectors: true
