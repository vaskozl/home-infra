# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/master/statefulset-apps-v1.json
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: claude-reviewer-sonnet
spec:
  template:
    spec:
      containers:
        - name: claude
          args: [reviewer]
          env:
            - name: ANTHROPIC_MODEL
              value: sonnet
      volumes:
        - name: ralph
          configMap:
            name: ralph
            defaultMode: 0555
