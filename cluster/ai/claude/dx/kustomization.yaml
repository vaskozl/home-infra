# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - sts.yaml
  - scaledobject.yaml
components:
  - ../components/common
  - ../components/chromium
labels:
  - pairs:
      app.kubernetes.io/instance: claude-dx-sonnet
    includeSelectors: true
