# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mx
  namespace: mailserver
spec:
  interval: 1h
  chart:
    spec:
      interval: 1h
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 3.4.0
  values:
    controllers:
      maddy:
        containers:
          app:
            image:
              repository: ghcr.io/vaskozl/maddy
              tag: 0.7.1@sha256:da029d78f0b5710f8b232649168332688ef46e8677a9cb7dab63ae46fbe0fea4
            args:
              - '-config=/etc/maddy/maddy.conf'
              - 'run'
            envFrom:
            - secretRef:
                name: maddy-secrets
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              limits:
                memory: 160Mi
              requests:
                cpu: 10m
                memory: 160Mi
            securityContext:
              readOnlyRootFilesystem: true
              capabilities:
                add:
                - NET_BIND_SERVICE
                drop: [ALL]
              runAsUser: 0
              runAsGroup: 0
              seccompProfile:
                type: RuntimeDefault
          f2b:
            image:
              repository: ghcr.io/vaskozl/fail2ban
              tag: 1.1.0@sha256:86a8682ce1a91063e1e61687e503508b0e2d8214a84539052455f40e81d6f13a
            resources:
              limits:
                memory: 30Mi
              requests:
                cpu: 10m
                memory: 30Mi
            securityContext:
              readOnlyRootFilesystem: true
              capabilities:
                add:
                - NET_ADMIN
                - NET_RAW
                drop: [ALL]
              runAsUser: 0
              runAsGroup: 0
        statefulset:
          podManagementPolicy: Parallel
          volumeClaimTemplates:
          - accessMode: ReadWriteOnce
            globalMounts:
            - path: /var/lib/maddy
            name: maddy-data
            size: 1Mi
            storageClass: nfs-client
        type: statefulset
      rspamd:
        containers:
          app:
            image:
              repository: ghcr.io/vaskozl/rspamd
              tag: 3.9.1@sha256:907b3cb1240022dffa464b73c19cb96cac9281d9699f524184d9c732405f9e78
            probes:
              liveness:
                enabled: true
                type: HTTP
                path: /ping
              readiness:
                enabled: true
                type: HTTP
                path: /ping
            resources:
              limits:
                memory: 180Mi
              requests:
                cpu: 10m
                memory: 180Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop: [ALL]
              runAsUser: 568
              runAsGroup: 568
              seccompProfile:
                type: RuntimeDefault
        statefulset:
          podManagementPolicy: Parallel
          volumeClaimTemplates:
          - accessMode: ReadWriteOnce
            globalMounts:
            - path: /var/lib/rspamd
            name: rspamd-data
            size: 1Mi
            storageClass: nfs-client
        type: statefulset

    persistence:
      maddy-config:
        name: maddy-config
        type: configMap
        globalMounts:
        - path: /etc/maddy
      rspamd-config:
        name: rspamd-config
        type: configMap
        globalMounts:
        - path: /etc/rspamd/local.d
        - path: /etc/rspamd/rspamd.local.lua
          subPath: rspamd.local.lua
      f2b-config:
        name: f2b-config
        type: configMap
        globalMounts:
        - path: /etc/fail2ban/fail2ban.conf
          subPath: fail2ban.conf
      f2b-filter:
        name: f2b-filter
        type: configMap
        globalMounts:
        - path: /etc/fail2ban/filter.d
      f2b-jail:
        name: f2b-jail
        type: configMap
        globalMounts:
        - path: /etc/fail2ban/jail.d
      tls:
        name: mail-sko-ai-tls
        type: secret
        globalMounts:
        - path: /etc/maddy/certs/mail.sko.ai
      run-maddy:
        type: emptyDir
        globalMounts:
        - path: /run/maddy
      logs:
        type: nfs
        server: syno.sko.ai
        path: /volume3/k8s-logs/mailserver
        advancedMounts:
          maddy:
            f2b:
              - path: /var/log/mailserver
                readOnly: true
    service:
      maddy:
        annotations:
          coredns.io/hostname: mail
        controller: maddy
        ports:
          smtp:
            primary: true
            port: 25
            nodePort: 31025
          imap:
            port: 143
          smtp-secure:
            port: 465
          smtp-auth:
            port: 587
          imap-secure:
            port: 993
          relay:
            port: 2225
        type: LoadBalancer
        loadBalancerIP: ${EI_MAILSERVER}
      rspamd:
        controller: rspamd
        ports:
          http:
            port: 11333
