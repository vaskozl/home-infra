# surbl.org does not answer unregistered resolvers, so every lookup hangs
# until timeout and returns SERVFAIL without ever contributing to scoring.
rules {
  "SURBL_MULTI" {
    disabled = true;
  }
  "SURBL_HASHBL" {
    disabled = true;
  }
}
