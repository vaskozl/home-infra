---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mailserver
resources:
- hr.yaml

configMapGenerator:
- name: maddy-config
  files:
  - maddy.conf
secretGenerator:
- name: maddy-secrets
  envs:
  - secrets.env

configurations:
  - kustomizeconfig.yaml
