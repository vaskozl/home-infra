# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: apks
  annotations:
    patch.sko.ai/app-template: common
spec:
  interval: 1h
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 4.2.0
  values:
    controllers:
      main:
        containers:
          app:
            image:
              repository: ghcr.io/vaskozl/nginx
              tag: 1.29.0@sha256:dcd355dc6e4a68125f2e7d1a137e23d130dd7d7599ddfc1fd6c5074dcc54e278
            resources:
              limits:
                memory: 30Mi
              requests:
                cpu: 15m
                memory: 30Mi
            securityContext:
              readOnlyRootFilesystem: false
              runAsUser: 33
              runAsGroup: 33
        type: statefulset
    defaultPodOptions:
      securityContext:
        fsGroup: 568
        runAsGroup: 568
        runAsNonRoot: true
        runAsUser: 568
        seccompProfile:
          type: RuntimeDefault
      shareProcessNamespace: true
    ingress:
      app:
        hosts:
          - host: apks.sko.ai
            paths:
              - path: /
                service:
                  identifier: app
                  port: http
    persistence:
      apks:
        existingClaim: apk-repo-pvc
        advancedMounts:
          main:
            app:
              - path: /usr/share/nginx/html
      run:
        type: emptyDir
    service:
      app:
        controller: main
        ports:
          http:
            port: 8080
