# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  annotations:
    patch.sko.ai/app-template: common
  name: apks
spec:
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 4.5.0
  interval: 24h
  values:
    controllers:
      main:
        containers:
          app:
            image:
              repository: ghcr.io/vaskozl/nginx
              tag: 1.29.4@sha256:3ae6da3337c64be42b9f33e8146c5df6b1c8c0fcf41630c7d4e640b80d49a199
            resources:
              limits:
                memory: 60Mi
              requests:
                cpu: 15m
                memory: 60Mi
            securityContext:
              readOnlyRootFilesystem: false
              runAsGroup: 33
              runAsUser: 33
        type: statefulset
    defaultPodOptions:
      securityContext:
        fsGroup: 568
        runAsGroup: 568
        runAsNonRoot: true
        runAsUser: 568
        seccompProfile:
          type: RuntimeDefault
    persistence:
      apks:
        advancedMounts:
          main:
            app:
              - path: /usr/share/nginx/html
        existingClaim: apk-repo-pvc
      run:
        type: emptyDir
    route:
      app:
        hostnames:
          - apks.sko.ai
        parentRefs:
          - name: internal-noauth
            namespace: agentgateway
    service:
      app:
        controller: main
        ports:
          http:
            port: 8080
