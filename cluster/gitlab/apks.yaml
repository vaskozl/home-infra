# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  annotations:
    patch.sko.ai/app-template: common
  name: apks
spec:
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 4.6.2
  interval: 24h
  values:
    controllers:
      main:
        containers:
          app:
            image:
              repository: ghcr.io/vaskozl/nginx
              tag: 1.29.5@sha256:034ed131695fe17752c4a2129a14dd22e0a263db29ae279c1b88ca4c362327ed
            resources:
              limits:
                memory: 60Mi
              requests:
                cpu: 15m
                memory: 60Mi
            securityContext:
              readOnlyRootFilesystem: false
              runAsGroup: 33
              runAsUser: 33
        type: statefulset
    defaultPodOptions:
      securityContext:
        fsGroup: 568
        runAsGroup: 568
        runAsNonRoot: true
        runAsUser: 568
        seccompProfile:
          type: RuntimeDefault
    persistence:
      apks:
        advancedMounts:
          main:
            app:
              - path: /usr/share/nginx/html
        existingClaim: apk-repo-pvc
      run:
        type: emptyDir
    route:
      app:
        hostnames:
          - apks.sko.ai
        parentRefs:
          - name: internal-noauth
            namespace: agentgateway
    service:
      app:
        controller: main
        ports:
          http:
            port: 8080
