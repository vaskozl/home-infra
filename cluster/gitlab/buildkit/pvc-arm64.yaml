---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: buildkit-arm64-pvc-v0
spec:
  storageClassName: nfs-client
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
