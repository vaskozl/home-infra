# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  annotations:
    patch.sko.ai/app-template: common
  name: dossier
  namespace: dossier
spec:
  chart:
    spec:
      chart: app-template
      interval: 24h
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 5.0.1
  interval: 24h
  values:
    controllers:
      api:
        type: deployment
        replicas: 1
        pod: &pod
          hostUsers: false
          securityContext:
            fsGroup: 568
            fsGroupChangePolicy: OnRootMismatch
        containers:
          app:
            # The ingest loops run on every non-worker tier, so api carries the
            # same live-source toggles as the worker.
            env: &env
              SAFETY_GATE_LIVE: "1"
              EURLEX_LIVE: "1"
              DG_GROW_LIVE: "1"
              # The OIDC callback and the DPP passport URLs are derived from this,
              # so it must be the externally reachable root, not the listen addr.
              DOSSIER_PUBLIC_URL: https://dossier.sko.ai
            # OIDC_CLIENT_SECRET for the `pi` AuthProvider declared in rekon's
            # examples/dossier/config/providers.yaml. Absent, dossier falls back
            # to its embedded any-username dev mock -- so this secret is what
            # makes the deployment actually authenticated.
            envFrom: &envFrom
              - secretRef:
                  name: oidc
            image: &image
              repository: registry.sko.ai/doudous/rekon/dossier
              tag: 0.0.41
            args:
              - -redis
              - redis-master:6379
              - -v
              - -mode
              - api
            resources:
              limits:
                memory: 512Mi
              requests:
                cpu: 700m
                memory: 128Mi
            securityContext: &security-context
              allowPrivilegeEscalation: true
              procMount: Unmasked
              runAsGroup: 568
              runAsUser: 568
              seccompProfile:
                type: Unconfined
      main:
        type: statefulset
        # app-template appends the controller identifier to the resource name
        # once a release has more than one controller. Pinning it keeps the
        # existing home-dossier-0 PVC bound.
        forceRename: dossier
        pod: *pod
        statefulset:
          volumeClaimTemplates:
            - accessMode: ReadWriteOnce
              globalMounts:
                - path: /home/nonroot
              name: home
              size: 20Gi
        containers:
          app:
            env:
              <<: *env
              GOMAXPROCS: "2"
            envFrom: *envFrom
            image: *image
            args:
              - -redis
              - redis-master:6379
              - -v
              - -mode
              - worker
            resources:
              limits:
                memory: 2Gi
              requests:
                cpu: 2
                memory: 512Mi
            securityContext: *security-context
    defaultPodOptions:
      imagePullSecrets:
        - name: regcred
      nodeSelector:
        kubernetes.io/arch: amd64
    persistence:
      api-home:
        type: emptyDir
        sizeLimit: 64Mi
        advancedMounts:
          api:
            app:
              - path: /home/nonroot
      turn-fifo:
        type: emptyDir
        medium: Memory
        sizeLimit: 1Mi
        advancedMounts:
          main:
            app:
              - path: /run/claude
    route:
      app:
        hostnames:
          - dossier.sko.ai
        parentRefs:
          - name: internal
            namespace: agentgateway
        rules:
          - backendRefs:
              - identifier: app
                port: 8080
    service:
      app:
        controller: api
        ports:
          http:
            port: 8080
