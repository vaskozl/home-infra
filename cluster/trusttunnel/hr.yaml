# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: trusttunnel
  annotations:
    patch.sko.ai/app-template: common
spec:
  interval: 24h
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 4.6.2
  values:
    controllers:
      trusttunnel:
        replicas: 1
        containers:
          app:
            image:
              repository: ghcr.io/vaskozl/trusttunnel
              tag: 1.0.33@sha256:e34b284416c208420c7abdb3a4d9fae4ba17756e1408fd286600e87bac1e973b
            command: [trusttunnel, /config/vpn.toml, /config/hosts.toml]
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: true
            resources:
              limits:
                memory: 150Mi
              requests:
                cpu: 30m
                memory: 150Mi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
              readOnlyRootFilesystem: true
              sysctls:
              - name: net.ipv4.ip_unprivileged_port_start
                value: "80"
        statefulset:
          podManagementPolicy: Parallel
          volumeClaimTemplates:
            - accessMode: ReadWriteOnce
              globalMounts:
                - path: /config
              name: config
              size: 1Gi
        type: statefulset
    defaultPodOptions:
      securityContext:
        fsGroup: 568
        runAsGroup: 568
        runAsNonRoot: true
        runAsUser: 568
        seccompProfile:
          type: RuntimeDefault
    route:
      app:
        hostnames:
          - trust.sko.ai
        parentRefs:
          - name: public
            namespace: agentgateway
        rules:
          - matches:
              - path:
                  value: /
            backendRefs:
              - identifier: app
                port: 443

    service:
      app:
        controller: trusttunnel
        ports:
          http:
            port: 443
