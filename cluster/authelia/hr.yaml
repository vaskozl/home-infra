apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: authelia
  annotations:
    patch.sko.ai/app-template: statefulset
spec:
  values:
    image:
      repository: ghcr.io/vaskozl/authelia-bin
      tag: 4.37.5@sha256:254adc4260db564e9fa3da8c29d6759d0a463a9001ad03483bbbc7c7a53d0572
    args: ['--config', '/config/configuration.yml']
    persistence:
      config:
        enabled: true
        type: configMap
        name: authelia-config
        mountPath: /config
    service:
      main:
        enabled: true
        type: LoadBalancer
        externalTrafficPolicy: Local
        ports:
          http:
            enabled: true
            port: 8080
    ingress:
      main:
        enabled: true
        annotations:
          nginx.ingress.kubernetes.io/enable-global-auth: 'false'
        hosts:
        - host: gate.sko.ai
          paths:
          - path: /
    resources:
      requests:
        cpu: 15m
        memory: 20Mi
      limits:
        cpu: 1
        memory: 200Mi
    securityContext:
      runAsUser: &uid 4000
      runAsGroup: *uid
      fsGroup: *uid

