---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: authelia
spec:
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 3.1.0
  values:
    controllers:
      authelia:
        containers:
          app:
            args:
            - --config
            - /config/configuration.yml
            image:
              repository: ghcr.io/vaskozl/authelia-bin
              tag: 4.38.8@sha256:bd9ae6043789198637cdcaa27a6dde572c5951ff07edf28f5602435d6fa4b889
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              limits:
                cpu: 1
                memory: 200Mi
              requests:
                cpu: 15m
                memory: 40Mi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                - ALL
              readOnlyRootFilesystem: true
    defaultPodOptions:
      securityContext:
        fsGroup: 568
        runAsGroup: 568
        runAsNonRoot: true
        runAsUser: 568
        seccompProfile:
          type: RuntimeDefault
    ingress:
      app:
        className: haproxy-internal
        hosts:
        - host: gate.sko.ai
          paths:
          - path: /
            service:
              identifier: app
              port: http
    persistence:
      config:
        globalMounts:
        - path: /config
        name: authelia-config
        type: secret
      tmp:
        type: emptyDir
    service:
      app:
        controller: authelia
        enabled: true
        ports:
          http:
            enabled: true
            port: 8080
