apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-v4
  namespace: authelia
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
