apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wireguard-v2
  namespace: wireguard
spec:
  storageClassName: "nfs-client"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10M
