apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wireguard 
  namespace: wireguard
spec:
  storageClassName: "rook-ceph-block"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10M
