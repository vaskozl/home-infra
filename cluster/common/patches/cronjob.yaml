apiVersion: batch/v1
kind: CronJob
metadata:
  name: any
spec:
  jobTemplate:
    spec:
      template:
        spec:
          shareProcessNamespace: true
          dnsConfig:
            options:
              - name: ndots
                value: "1"
