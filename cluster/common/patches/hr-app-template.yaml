---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: stub
spec:
  chart:
    spec:
      chart: app-template
      version: 3.0.4
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    env:
      TZ: Europe/London
    persistence:
      tmp:
        enabled: true
        mountPath: /tmp
        type: emptyDir
        medium: Memory
