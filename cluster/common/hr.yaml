apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: stub
spec:
  interval: 30m
  maxHistory: 2
  install:
    createNamespace: true
    timeout: &timeout 15m0s
    remediation:
      retries: 1
  upgrade:
    cleanupOnFail: true
    timeout: *timeout
    remediation:
      retries: 1
  uninstall:
    keepHistory: false
