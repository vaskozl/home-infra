# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  annotations:
    patch.sko.ai/app-template: common
  name: headlamp
spec:
  chart:
    spec:
      chart: app-template
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 4.5.0
  interval: 1h
  values:
    controllers:
      headlamp:
        containers:
          app:
            args:
              - -in-cluster
              - -plugins-dir=/build/plugins
              - -oidc-client-id=$(OIDC_CLIENT_ID)
              - -oidc-client-secret=$(OIDC_CLIENT_SECRET)
              - -oidc-idp-issuer-url=$(OIDC_ISSUER_URL)
              - -oidc-scopes=$(OIDC_SCOPES)
            envFrom:
              - secretRef:
                  name: oidc
            image:
              repository: ghcr.io/headlamp-k8s/headlamp
              tag: v0.39.0@sha256:c8c564ffbf088955efa4d08523df1b76e3bc4594efaf85efe9996ab99ebe48b2
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              limits:
                memory: 30Mi
              requests:
                cpu: 15m
                memory: 30Mi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                add:
                  - NET_BIND_SERVICE
                drop:
                  - ALL
        initContainers:
          plugins:
            command:
              - /bin/sh
              - -c
              - mkdir -p /build/plugins && cp -r /plugins/* /build/plugins/
            image:
              repository: ghcr.io/headlamp-k8s/headlamp-plugin-flux
              tag: v0.5.0@sha256:05c3df203b2ff82e09e944e31cd16ab66429f37df7566ba0d47285add7c78c5a
        statefulset:
          podManagementPolicy: Parallel
        type: statefulset
    defaultPodOptions:
      nodeSelector:
        kubernetes.io/arch: amd64
      securityContext:
        fsGroup: 101
        runAsGroup: 101
        runAsNonRoot: true
        runAsUser: 100
        seccompProfile:
          type: RuntimeDefault
    persistence:
      config:
        globalMounts:
          - path: /build/plugins
        type: emptyDir
    route:
      app:
        hostnames:
          - headlamp.sko.ai
        parentRefs:
          - name: internal-noauth
            namespace: agentgateway
    service:
      app:
        controller: headlamp
        ports:
          http:
            port: 4466
            primary: true
